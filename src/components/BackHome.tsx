import ArrowLink from '@/components/links/ArrowLink';

export default function BackHome() {
  return (
    <ArrowLink direction='left' className='mb-5 mt-5' href='/'>
      Back to Home
    </ArrowLink>
  );
}
